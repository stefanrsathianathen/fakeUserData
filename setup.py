from distutils.core import setup
setup(
  name = 'fakeUserData',
  packages = ['fakeUserData'], # this must be the same as the name above
  version = '1.0.3',
  description = 'Generate fake user data',
  author = 'Stefan R. Sathianathen',
  author_email = 'stefan4419@gmail.com',  
  url = 'https://github.com/stefanrsathianathen/fakeUserData', # use the URL to the github repo
  download_url = 'https://github.com/stefanrsathianathen/fakeUserData/archive/1.0.3.tar.gz', # I'll explain this in a second
  keywords = ['testing', 'userData', 'data'], # arbitrary keywords
  classifiers = [],
)